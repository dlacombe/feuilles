<h1>Liste des feuilles</h1>
<% if notice.nil? == false %>
    <p id="notice"><%= notice %></p>
<% end %>
<table>
  <% @feuilles.each do |feuille| %>
      <% client = Client.find(feuille.client_id)%>
      <tr>
        <td><%= link_to feuille.debut.strftime("%Y/%m/%d") %></td>
        <td><%= content_tag('pre',feuille.travail_effectuer) %></td>
        <td><%= link_to client.nom %></td>
        <td><%= link_to 'Détruire', feuille, confirm: 'Êtes-vous certain?', method: :delete %></td>
      </tr>

  <% end %>
</table>

<br />
<%= link_to 'Menu des Clients', clients_path %>